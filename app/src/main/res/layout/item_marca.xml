<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="@dimen/rounded_corner_small">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground"
        android:background="?android:selectableItemBackground"
        android:paddingBottom="@dimen/padding_general_small">

        <ImageView
            android:id="@+id/img_marca"
            android:layout_width="match_parent"
            android:layout_height="@dimen/image_detail_small"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:background="@color/white"
            app:srcCompat="@drawable/app_icon"
            android:layout_alignParentLeft="true" />

        <!--<de.hdodenhof.circleimageview.CircleImageView
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:id="@+id/img_logo"
            android:layout_width="@dimen/distributor_image_logo_size"
            android:layout_height="@dimen/distributor_image_logo_size"
            android:layout_marginBottom="@dimen/margin_between_textView"
            android:layout_centerHorizontal="true"
            android:layout_alignBottom="@+id/img_distributor"
            android:scaleType="centerCrop"
            android:src="@drawable/xalesmap_icon"
            app:civ_circle_background_color="@color/white"
            app:civ_border_width="2dp"
            app:civ_border_color="@color/white"/>-->
        <TextView
            android:id="@+id/tv_nombre"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/img_marca"
            android:layout_centerHorizontal="true"
            android:textSize="@dimen/text_size_small"
            android:textStyle="bold"
            android:text="Distributor X" />
        <!--<TextView
            android:id="@+id/tv_ruc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/tv_name"
            android:layout_centerHorizontal="true"
            android:text="Distributor X" />-->

    </RelativeLayout>

</androidx.cardview.widget.CardView>